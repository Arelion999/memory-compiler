# Запинено на 1.29.1 — ветка 1.x закрыта на 1.29.0 (28.07.2026), дальше только
# maintenance-бэкпорты; 1.29.1 (24.08.2026) как раз такой: лимит тела запроса
# распространён на SSE- и OAuth-эндпоинты (#3344), object-rooted outputSchema для
# рекурсивных типов (#3377), достроенная модель FastMCP Settings при импорте (#3352).
# Отдельного advisory на 1.29.0 нет — ни один из шести GHSA репозитория её не задевает
# (последний, GHSA-vj7q-gjh5-988w, закрыт ещё в 1.28.1), так что это hardening, не CVE.
# Это осознанная конечная станция: 2.0.0 вышел в тот же день, но миграция на него
# переписывает FastMCP→MCPServer, декораторы Server→параметры конструктора и слой
# транспортов, то есть api.py/tools.py/handlers.py и весь сьют, — при нулевой выгоде
# для нас (см. разбор P3). Раньше был не пиннутый >=1.0.0 → невоспроизводимые сборки.
# Совместимость проверена полным сьютом на связке 1.29.1 + starlette 1.0.0, плюс
# точечно: SseServerTransport.connect_sse, StreamableHTTPSessionManager, декораторы
# Server (resources/prompts/completion), ToolAnnotations, outputSchema.
# ⚠️ Две несущие детали поведения pydantic-моделей SDK, на которых мы стоим и которые
# перепроверены зондом НА 1.29.1 (обе молчаливые — сломаются без исключения):
#   ClientCapabilities.model_config extra="allow" — иначе слепнет зонд MCP Apps;
#   у Tool НЕ выставлен populate_by_name — значит _meta ставится только по алиасу.
mcp[cli]==1.29.1
uvicorn>=0.30.0
# Запинено на 1.0.0 (проверенная на NAS версия). Starlette 1.0 требует, чтобы
# endpoint возвращал Response — handle_sse это делает (см. фикс -32602 v1.7.34).
# Не откатывать без проверки: разные ветки Starlette по-разному завершают SSE.
starlette==1.0.0
whoosh>=2.7.4
markdown-it-py>=3.0.0
pygments>=2.17.0
nh3>=0.2.17
sentence-transformers>=2.2.0
numpy>=1.24.0
cryptography>=42.0.0
pyyaml>=6.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
