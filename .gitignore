# Secrets
.env
.env.*
!.env.example
# Бэкапы (.env.bak-*, docker-compose.yml.bak-* и т.п.) — могут содержать секреты
*.bak*

# Деплой-скрипты с credentials
deploy.py
deploy_image.py
setup_nas.py
deploy.bat

# Docs (не для публикации)
docs/description.md
docs/superpowers/
docs/_internal/

# Node (docx builder)
docs/node_modules/
docs/package.json
docs/package-lock.json

# Python
__pycache__/
*.pyc

# Knowledge base (отдельный git в /knowledge)
knowledge/
backups/

# Claude
CLAUDE.md
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Конфликтные копии Synology Drive (<имя>_<ПК>_<дата>_Conflict.<ext>).
# Каталог живёт внутри синхронизируемой папки, и такие дубли появляются сами.
# Без этой строки `git add -A` в scripts/release.* утащил бы устаревшую копию
# README в публичный репозиторий — один такой файл уже лежал рядом.
*_Conflict.*

# Test
test_all.py
